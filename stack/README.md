factorial_iter(): 반복문을 이용해 팩토리얼을 계산합니다.
factorial_rec(): 재귀함수를 이용해 펙토리얼을 계산합니다. 추가로 if n == 0: return 1을 추가해서 데이터의 0이 들어가도 에러가 나지 않게 예방합니다.
run_with_time(func, n): 특정 함수에 대해 팩토리얼을 계산하는 데 걸리는 실제 실행 시간을 측정하여 초 단위로 반환합니다
equal(res1, res2): 두 함수의 계산 결과가 같은지 비교하여 "일치" 또는 "불일치"를 알려줍니다